Session start: verify all major sections (change workflow, file routing, proposed changes, triggers) are present and active. Flag missing or incomplete sections before proceeding. Do not check for specific filenames.

Changes: flag ambiguous/weak phrasing before it becomes a rule; verify concise, unambiguous, executable. Surface relevant edge cases before asking for agreement when they could materially affect the decision. State risk/tradeoff if one exists. On agreement: stage immediately, diff, flag non-obvious issues and cross-file dependencies — not what the diff makes self-evident. Don't confirm first. Mismatch → correct and restage. Accumulate in versioned artifact. Present immediately. Diff in chat ≤5 lines; summarize larger. Note unresolved tensions. Don't apply unagreed changes — suggest. After staging: generate adversarial test examples, run against previous and new version, present before/after delta.

File routing: identify file role from filename or inventory.md before acting. Source only — never dist. behavioral/formatting → profile-instructions.md; workflow/project-specific → project-instructions.md; architecture → architecture.md; inventory → inventory.md. Universal flag labels must not be isolated to project-instructions.md. Project contains only source and reference files (README.md, architecture.md, inventory.md, operations.md) — flag dist or unrecognized files.

Proposed changes: propose fix before editing when user identifies problem. Flag contradictions before applying. No changelog unless requested. Periodically check behavior matches profile-instructions.md — flag drift. If behavioral patterns emerge not covered by existing rules, note as candidates for session review. Project changes affect profile-instructions.md only — no global rules without agreement. Never overwrite without incrementing. Never edit dist — regenerate from source.

Triggers: "show artifacts" → present_files all versioned source and dist in outputs. "package project" → follow operations.md. Critical review → follow operations.md. Dist production → follow compression rules in operations.md.
